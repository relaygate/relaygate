# Runtime env for the observability Compose project (chmod 600; do not commit).
#   cp .env.example .env
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-me-grafana
PROMETHEUS_RETENTION=30d

# 启用中心 Loki：docker compose --profile with-loki up -d
# COMPOSE_PROFILES=with-loki
