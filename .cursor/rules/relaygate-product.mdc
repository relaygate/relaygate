---
description: RelayGate 产品定位、面向运维文案、敏感操作二次确认（始终生效）
alwaysApply: true
---

# RelayGate 产品规则

本项目是基于 Envoy 的 **通用 L4 网关**（南北向转发、多节点机群、xDS 热更新），**不是**某款游戏或某一客户的专用产品。

开源仓库卫生、发布产物与文档分层见 **`relaygate-oss.mdc`**；贡献/DCO/Issue 模板细则见 **`relaygate-contributing.mdc`**。

**对内双组件（单一产品，非双产品线）**：主控（control）= Panel + 意图/发布/节点名册 + 可选本机转发；节点（node）= agent + 本机热更新 + Envoy（无完整 Panel）。

## 术语表（分层；勿混用）

**节点是角色，agent 是节点上的守护进程**——二者并存，不要为统一而把安装改成 `agent`，也不要大规模把 `core/agent` 改成 `core/node`。

| 层级 | 用户可见（中/英） | 标识符 / 路径 | 勿再用 |
|------|------------------|---------------|--------|
| 安装角色 | 主控 / control；节点 / node（网关节点） | `install.sh control` · `install.sh node …` · `packaging/{control,node}/` | primary、secondary；**勿用 agent 作安装角色名** |
| 升级 | 升级 | `install.sh upgrade`（读现有 `.env` 保留角色） | 长串 `env KEY=… bash -s -- -y` |
| 守护进程 | （运维文案说「节点拉取 / 心跳」；高级/命令可写 agent） | `relaygate agent`；systemd `relaygate-agent`；包 `core/agent`；HTTP `/api/agent/*` | 把 agent 说成与主控对等的「第二种产品角色」 |
| 节点凭证 / 主控地址 | — | `AGENT_TOKEN` / `AGENT_TOKEN_FILE`；`CONTROL_URL` | `PRIMARY_URL` |
| Panel 启停 / 绑定 | — | `PANEL_ENABLED` · `PANEL_BIND` · `PANEL_ROLE` | `ENABLE_PANEL` |
| Grafana 启停 | — | `GRAFANA_ENABLED`（对齐 `GRAFANA_*`） | `ENABLE_GRAFANA`；勿用 `PANEL_GRAFANA_ENABLED` |
| Panel 写保护 | 主控 / 只读 | 内部 `PANEL_ROLE=primary\|standby` | 对外说「secondary」 |
| 安全落地（分层） | — | `APPLY_FIREWALL`（安装/CLI 一次性）；`SECURITY_AUTO_APPLY`（拉取后主机侧） | 勿把二者当别名混用 |

机群稳态：节点上的 **agent** 拉取配置版本 → 本机落盘 → 本机热更新；SSH 不进正式菜单。

## 产品定位

- 文档、UI、CLI、注释、示例、提交说明中，**不要**使用游戏或真实业务向表述。
- 术语对齐 Envoy / L4：**下游 / Downstream**、**上游 / Upstream**、**入口 / Listener**、**转发 / Forward**、**网关 / Gateway**。
- 示例用 RFC 文档地址与占位名：`203.0.113.x`、`server-01`、`gateway-01`、`example.com`；勿写真实厂商、客户名或业务隐喻。
- 新文案 / 字段 / i18n key **禁止**以 `game` / `player` /「玩家」/「游戏服」等作产品主词。
- 写作用左列：客户端/下游（非玩家）；上游服务（非区服/房间服）；业务/项目名（非 `game_*`）；网关/机群（非备服/开服等黑话）。
- YAML 已有标识符（如 `game_name`、`backend_*`）可兼容保留；展示层用中性词；勿擅自全仓重命名。新能力一律中性命名。

## 用户文案

UI、i18n、Panel、确认框、EmptyState、toast、CLI 帮助等**面向运维用户**：用业务/运维语言，不用开发黑话。

- 主文案说清：**要做什么 / 为什么失败 / 下一步怎么做**。
- 错误提示须可行动；勿单独甩 `exit status 1`、`ENOENT`、404、stack 等。技术细节放折叠区、日志或「高级/命令参考」。
- 避免作主文案：API 路径、HTTP 状态码、JSON、goroutine、包名、裸露路径（如 `DataDir`）、Promise/null 等。

```text
❌ GET /api/v1/fleet 404，ENOENT
✅ 未找到该舰队配置。请确认名称后重试，或到舰队列表查看已有配置。
```

## 敏感操作确认

写操作 / 危险操作须用户**显式二次确认**（输入确认词或等价开关），**禁止**一键静默执行。

- 确认框文案：**一句话**说明要做什么；若**影响连接**，再补**一句红字**说明断连风险。
- 确认输入统一为中文 **`确认`** 或英文 **`Confirm`**（`Confirm` 大小写敏感；后端两者都接受）。**不再**使用按操作区分的指令词（如已废除的 `HOT_APPLY` / `RELOAD_ENVOY` / `PUBLISH_FLEET` 等）。
- **standby / 只读角色禁止写操作**；须引导到主控。
- CLI：危险操作须交互确认，或显式开关（如 `relaygate reload --hard`、`FIREWALL_CONFIRM=Confirm`、`RELAYGATE_CONFIRM=Confirm`）；避免默认真销毁。
- 接入 `fleet join` 无需确认词（主 UX 为一句话安装命令）。

```text
❌ 将当前业务配置发布为机群新版本。请输入 PUBLISH_FLEET 确认。
✅ 将当前业务配置发布为机群新版本，各节点自行拉取并应用。请输入「确认」或 Confirm。
```

机群发布入口为 CLI `relaygate fleet publish`（及 API）；Panel「配置应用」不做全机群发布；立即对齐用机群页按台「同步」。

## Panel 操作按钮风险色

- **红（destructive）**：会断连 / 破坏性 / 不可轻易撤销（硬重启本机应用、退役节点、摘流、回滚、应用防火墙 flush、删除上游等）。
- **橙（caution）**：有影响但非立刻全断，或需留意（热更新本机应用、单节点同步、恢复承接、应用档位等）。
- **灰（outline / secondary）**：常规保存、生成接入命令、只读刷新、检查/诊断等。
