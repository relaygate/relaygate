# RelayGate: Envoy 数据面 + 可观测性 + Go Panel
# 每台网关共用本模板，靠 .env 中的 GATEWAY_NAME 等变量区分实例。
# 在仓库根执行:
#   docker compose -f deploy/compose.yaml --env-file .env up -d
#
# Panel 默认启用（COMPOSE_PROFILES 含 with-panel）。
# 从节点可去掉 with-panel，仅跑数据面 + 本地 Prometheus（remote_write 到中心）。

name: ${COMPOSE_PROJECT_NAME:-relaygate}

services:
  envoy:
    image: ${ENVOY_IMAGE:-envoyproxy/envoy:v1.39.0}
    container_name: ${GATEWAY_NAME:?set GATEWAY_NAME}-envoy
    network_mode: host
    restart: unless-stopped
    stop_grace_period: 45s
    stop_signal: SIGTERM
    volumes:
      - ../gateway/generated/envoy.yaml:/etc/envoy/envoy.yaml:ro
      - envoy-logs:/var/log/envoy
    command:
      - /usr/local/bin/envoy
      - -c
      - /etc/envoy/envoy.yaml
      - --service-cluster
      - ${GATEWAY_NAME}
      - --service-node
      - ${GATEWAY_NAME}
      - --log-level
      - info
      - --concurrency
      - "${ENVOY_CONCURRENCY:-0}"
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "bash -c 'exec 3<>/dev/tcp/127.0.0.1/${ENVOY_ADMIN_PORT:-9901} && printf \"GET /ready HTTP/1.0\\r\\n\\r\\n\" >&3 && cat <&3 | grep -q LIVE'",
        ]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 15s

  prometheus:
    image: prom/prometheus:v3.5.0
    container_name: ${GATEWAY_NAME}-prometheus
    network_mode: host
    restart: unless-stopped
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./prometheus/rules:/etc/prometheus/rules:ro
      - prometheus-data:/prometheus
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.path=/prometheus
      - --storage.tsdb.retention.time=${PROMETHEUS_RETENTION:-15d}
      - --web.listen-address=127.0.0.1:9090
      - --web.enable-lifecycle
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "3"

  grafana:
    image: grafana/grafana:12.1.0
    container_name: ${GATEWAY_NAME}-grafana
    network_mode: host
    restart: unless-stopped
    profiles: ["with-grafana"]
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:?set GRAFANA_ADMIN_PASSWORD in .env}
      GF_SERVER_HTTP_ADDR: 127.0.0.1
      GF_SERVER_HTTP_PORT: 3000
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_ANALYTICS_REPORTING_ENABLED: "false"
      GF_ANALYTICS_CHECK_FOR_UPDATES: "false"
      GF_LOG_MODE: console
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning:ro
      - grafana-data:/var/lib/grafana
    depends_on:
      - prometheus
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "3"

  node_exporter:
    image: prom/node-exporter:v1.9.1
    container_name: ${GATEWAY_NAME}-node-exporter
    network_mode: host
    pid: host
    restart: unless-stopped
    command:
      - --path.rootfs=/host
      - --web.listen-address=127.0.0.1:9100
      - --collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)
    volumes:
      - /:/host:ro,rslave
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "2"

  panel:
    build:
      context: ..
      dockerfile: Dockerfile.panel
    image: ${PANEL_IMAGE:-relaygate-panel}:${IMAGE_TAG:-local}
    container_name: ${GATEWAY_NAME}-panel
    network_mode: host
    restart: unless-stopped
    profiles: ["with-panel"]
    environment:
      # PANEL_ROLE 仅写在 .env 作运维约定（primary/standby），Panel 不读取；standby 勿开本 profile
      PANEL_ADMIN_PASSWORD: ${PANEL_ADMIN_PASSWORD:?set PANEL_ADMIN_PASSWORD in .env}
      PANEL_BIND: ${PANEL_BIND:-127.0.0.1:8080}
      PANEL_ROOT: /workspace
      GATEWAY_NAME: ${GATEWAY_NAME}
      ENVOY_ADMIN_URL: http://127.0.0.1:${ENVOY_ADMIN_PORT:-9901}
      PROMETHEUS_URL: http://127.0.0.1:9090
      RELAYGATE_BIN: /usr/local/bin/relaygate
    volumes:
      - ../config:/workspace/config
      - ../gateway:/workspace/gateway
      - ../deploy:/workspace/deploy
      - ../scripts:/workspace/scripts:ro
      - ../web:/workspace/web:ro
      - ../backups:/workspace/backups
      - ../.env:/workspace/.env:ro
      - /var/run/docker.sock:/var/run/docker.sock
    logging:
      driver: json-file
      options:
        max-size: "20m"
        max-file: "3"
    depends_on:
      - envoy
      - prometheus

volumes:
  envoy-logs:
  prometheus-data:
  grafana-data:
